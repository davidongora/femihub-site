deployment:
  tasks:
    - export DEPLOYPATH='/home6/femihub/public_html'
    - echo "Running build for Vite project..." >> /home6/femihub/deploy.log
    - pnpm install >> /home6/femihub/deploy.log 2>&1
    - pnpm run build >> /home6/femihub/deploy.log 2>&1
    - echo "Clearing current content in ${DEPLOYPATH}..." >> /home6/femihub/deploy.log
    - rm -rf ${DEPLOYPATH}/* >> /home6/femihub/deploy.log 2>&1
    - echo "Copying built files to ${DEPLOYPATH}..." >> /home6/femihub/deploy.log
    - cp -R dist/* ${DEPLOYPATH}/ >> /home6/femihub/deploy.log 2>&1
    - echo "Deployment completed." >> /home6/femihub/deploy.log
